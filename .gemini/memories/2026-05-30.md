# Memory: 2026-05-30

## 주요 작업 내역
### 1. 아키텍처 및 디자인 개편
- **Notion 스타일 사이드바**: 기존 상단 메뉴를 제거하고 좌측 고정 사이드바(접기/펴기 가능) 도입.
- **실시간 대시보드**: `/dashboard`를 메인으로 설정, Supabase 데이터를 활용한 통계 카드(총 이슈, 작성자 수 등) 구현.
- **인증 보안 강화**: 모든 페이지에 JWT 토큰 유효성 검증(Auth Guard) 적용.

### 2. 버그 수정 및 안정화
- **로그인 리디렉션 루프 해결**: 
  - `@supabase/ssr`의 `createBrowserClient`를 도입하여 세션을 쿠키와 동기화 (미들웨어 인식 문제 해결).
  - `auth/callback`에서 명시적인 `exchangeCodeForSession` 처리 추가.
  - 미들웨어 보호 대상에서 `/auth/callback` 제외.
- **빌드 에러 수정**: `Layout` 컴포넌트의 Props 변경 사항이 반영되지 않았던 `login.tsx` 타입 에러 해결.
- **하이드레이션 오류 수정**: `mounted` 상태 체크 로직을 Layout에 추가하여 서버/클라이언트 불일치 방지.

## 결과
- Vercel 프로덕션 배포 완료 및 구글 OAuth 로그인 정상 작동 확인.
- 미니멀하고 정교한 Linear 디자인 시스템 적용 완료.

### 3. 반응형 디자인 및 모바일 최적화
- **반응형 레이아웃**: 모바일용 햄버거 메뉴 및 오버레이 사이드바 도입.
- **그리드 시스템 최적화**: 대시보드 및 상세 페이지의 그리드 구성을 화면 크기에 따라 유동적으로 조정 (1~4컬럼).
- **데이터 노출 제어**: 좁은 화면에서 중요도가 낮은 메타데이터를 숨겨 가독성 향상.
