# Memory: 2026-09-05

## 주요 작업 내역

### 1. shadcn/ui Slate 테마 기반 백오피스 디자인 시스템 적용
- **공식 shadcn Dashboard 벤치마크**: `https://ui.shadcn.com/examples/dashboard` 구조를 1:1 매핑하여 엔터프라이즈 어드민 스타일로 전면 개편.
- **디자인 토큰 구축**:
  - `globals.css`에 Light/Dark 모드 공식 Slate CSS 변수 세트 정의 (`--background`, `--foreground`, `--card`, `--primary`, `--secondary`, `--muted`, `--accent`, `--border`, `--ring` 등).
  - `tailwind.config.ts`에 시맨틱 컬러 토큰 및 보더 라디우스 매핑.
- **`class-variance-authority` (cva) 도입**:
  - shadcn 컴포넌트 표준 variant 관리를 위해 cva 라이브러리 추가 및 연동.

### 2. shadcn/ui 코어 컴포넌트 라이브러리 구축 (`src/components/ui/`)
- **`button.tsx`**: `default`, `destructive`, `outline`, `secondary`, `ghost` variants 지원.
- **`card.tsx`**: `CardHeader`, `CardTitle`, `CardDescription`, `CardContent` 등 대시보드 레이아웃 필수 컴포넌트.
- **`badge.tsx`**: `default`, `secondary`, `destructive`, `outline`, `success`, `warning`, `info` 상태 뱃지.
- **`tabs.tsx`**: 대시보드 탭 네비게이션 (`Overview`, `Analytics`, `Reports`, `Notifications`) 지원.
- **`input.tsx`**: 백오피스 검색 및 필터 인풋.
- **`table.tsx`**: 고밀도 관리자 데이터 테이블 컴포넌트 (`Table`, `TableHeader`, `TableRow`, `TableHead`, `TableBody`, `TableCell`).
- **`avatar.tsx`**: 유저 및 기여자 프로필 표시용 아바타 컴포넌트.

### 3. 백오피스 레이아웃 & 내비게이션 개편 (`Layout.tsx`)
- **상단 글로벌 헤더 (Top Header)**:
  - 워크스페이스 스위처 배지 (`Acme BackOffice`).
  - 메인 네비게이션 링크 (`Overview`, `Management`).
  - 전역 검색창, 언어 토글, 다크/라이트 테마 토글, 알림 아이콘.
  - 관리자 아바타 및 드롭다운 메뉴 (프로필, Role 뱃지, 로그아웃 연동).
- **컴팩트 사이드바 (Sidebar)**:
  - 백오피스 메뉴 계층화 (`Platform`, `System`) 및 사이드바 접기/펼치기(Collapse) 지원.
  - 모바일 슬라이드오버 반응형 메뉴 완비.

### 4. 대시보드 페이지 리뉴얼 (`dashboard/index.tsx`)
- **헤더 액션 바**: 날짜 범위 필터, `Download Report`, `+ New Record` 버튼.
- **4대 KPI 메트릭 카드**: Total Posts, Active Authors, Today's Volume, Service Health (Supabase 99.9% Uptime).
- **2-컬럼 메인 그리드**:
  - **Overview (Col 4)**: shadcn 스타일의 월별 데이터 처리량 바 차트 (호버 툴팁 지원).
  - **Recent Activity (Col 3)**: 실시간 Supabase 포스트 목록 및 작성자/일자 뱃지.
- **서브 탭**: `Analytics`, `Reports`, `Notifications` 시스템 패널 구성.

### 5. 관리자 Tasks 테이블 리뉴얼 (`board/index.tsx`)
- 단순 피드 뷰에서 **shadcn Tasks 스타일의 Data Table**로 전면 전환.
- 전체 선택 및 개별 선택 체크박스 일괄 처리 지원.
- 실시간 타이틀/작성자 검색 필터 툴바 연동.
- 상태 뱃지 (`Active`, `Done`), 작성자 아바타, 정렬 인디케이터 지원.

### 6. 빌드 및 검증
- `pnpm build`: Next.js 16.1.6 (Turbopack) & React 19.2.3 컴파일 0 에러 무결성 검증 완료.

### 7. Supabase Keep-Alive 워크플로우 최적화 및 `main` 배포
- **원인 분석**: 
  - GitHub Actions의 60일 비활동 시 스케줄 워크플로우 자동 비활성화(Disabled) 확인.
  - Vercel 이전 배포 URL(`nextjs-supabase-examples-ten.vercel.app`) 404 오류 감지.
  - Actions `schedule`은 기본 브랜치(`main`)의 파일만 바라보는데 변경 사항이 `dev`에만 머물러 있던 문제 해결.
- **개선 조치 및 검증**:
  - Vercel 우회 구조를 제거하고 GitHub Actions가 Supabase REST API(`posts?limit=1`)를 직접 찌르도록 `.github/workflows/keep-alive.yml` 전면 개편.
  - `curl -f -s -S` 플래그를 추가하여 오류 감지 강화.
  - `dev` 브랜치를 `main` 브랜치로 머지 및 푸시 완료.
  - GitHub Actions 수동 실행(Run workflow) 결과 `Payload: [{"id":"..."}]` 정상 응답 및 성공 확인 완료.

---

## 📌 참고 사항: Supabase Keep-Alive 외부 무료 크론 연동 가이드

GitHub Actions는 **60일 동안 리포지토리에 활동(커밋/푸시 등)이 없으면 스케줄 워크플로우가 자동으로 비활성화(Disabled)**되는 정책이 있습니다. 장기 비활동 시에도 365일 영구적으로 Supabase의 7일 Pause를 방지하기 위해 아래 외부 무료 웹 크론 서비스를 백업으로 등록해두는 것을 권장합니다.

### 1. cron-job.org 연동 (완전 무료 / 강력 추천)
- **사이트**: https://cron-job.org
- **설정 값**:
  - **Title**: `Supabase Keep Alive`
  - **URL**: `https://dvzuibrnjxanuiqqqbjl.supabase.co/rest/v1/posts?select=id&limit=1`
  - **Request Method**: `GET`
  - **Schedule**: 매일 1회 또는 3일마다 1회 (User-defined)
  - **Headers**:
    - `apikey`: `<SUPABASE_ANON_KEY>`
    - `Authorization`: `Bearer <SUPABASE_ANON_KEY>`
- **장점**: GitHub 계정 활동 여부와 무관하게 정해진 스케줄대로 24시간 안정적으로 실행됨.

### 2. UptimeRobot 연동 (서버 상태 모니터링 겸용)
- **사이트**: https://uptimerobot.com
- **설정 값**:
  - **Monitor Type**: `HTTP(s)`
  - **Friendly Name**: `Supabase Health Check`
  - **URL (or IP)**: `https://dvzuibrnjxanuiqqqbjl.supabase.co/rest/v1/posts?select=id&limit=1`
  - **Monitoring Interval**: 1일 또는 12시간 주기
  - **Custom HTTP Headers**:
    - `apikey`: `<SUPABASE_ANON_KEY>`
- **장점**: 무료 50개 모니터 제공, 장애 발생 시 이메일 알림 수신 가능.
